{
  "name": "logparser",
  "version": "1.0.0",
  "description": "Expose pacfile and ip address internally for our Smoothwall Auto-Login functionality",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2",
    "nodemon": "^1.11.0"
  }
}
